extends "res://utility/scripts/cached_scenes_singleton.gd"

#var animation_cache := {}

#func get_animation(animation_name: String, scene_path: String) -> Animation:
	#var animation
	#
	#if animation_cache.has(animation_name):
		#animation = animation_cache[animation_name]
	#else:
		#var scene = get_cached_scene(scene_path)
		#if scene:
			#var animation_instance = scene.instantiate()
			#animation_instance.
			#cache_scene[path] = scene
			#
		#return animation
